# /wo — Turnover Work Order system (private, noindex)

- `/wo/muskego-6/` — the worker/iPad app (PIN-locked; first PIN = 1224, change via admin).
- `/wo/admin.html` — back office: worker roster, recurring-items brain, completed projects, tenant packet, cloud auto-upload endpoint.
- `apps-script/Code.gs` — paste into script.google.com to enable auto-upload of every stamp to Drive (see file header for the 5-step setup).
